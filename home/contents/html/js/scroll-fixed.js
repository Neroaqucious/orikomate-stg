jQuery(function($) {
    /*
    var nav = $('.entry .step-header', '.contents'),
    offset = nav.offset();

    $(window).scroll(function () {
        if($(window).scrollTop() > offset.top) {
            nav.css('position', 'fixed')
        } else {
            nav.css('position', 'absolute')
        }

    });
    */
});